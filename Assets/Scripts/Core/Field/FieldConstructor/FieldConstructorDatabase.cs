using System;
using System.Collections;
using System.Collections.Generic;
using Core.Characters;
using Core.Levels;
using Unity.VisualScripting;
using UnityEngine;

public class FieldConstructorDatabase : MonoBehaviour
{
    /*public Dictionary<(int, int), CharacterType> Characters = new Dictionary<(int, int), CharacterType>();

    private void OnEnable()
    {
        LoadConstructorDatabase();
    }

    private void OnDisable()
    {
        SveFieldConstructorDatabase();
    }

    public void SveFieldConstructorDatabase()
    {
        SaveSistem.SaveFieldConstructorDatabase(Characters);
    }

    public void LoadConstructorDatabase()
    {
        FieldConstructorData data = SaveSistem.LoadFieldConstructorDatabase();
        Characters = data.Characters;
    }*/
}

